#include <bits/stdc++.h>
using namespace std;

int main()
{
    char val = 'c';
    char upperCase = toupper(val);
    cout<<upperCase<<endl;
    // vector<char> values;
    // string s;
    // cin >> s;
    // for (int i = 0; i < s.size(); i++)
    // {
    //     if (s[i] != '+')
    //     {
    //         values.push_back(s[i]);
    //     }
    // }

    //     sort(values.begin(), values.end());
    //     for (int i = 0; i < values.size(); i++)
    //     {
    //         if (i !=( values.size() - 1)) cout <<values[i]<< "+";
    //         else cout << values[i];
        // }
    return 0;
}